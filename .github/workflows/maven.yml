name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
          java-package: jdk
      - name: Build, Package, Verify
        run: mvn --settings .github/workflows/settings.xml -B -P github clean package verify install
        env:
          MAVEN_USER: ${{ secrets.READ_PACKAGES_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.READ_PACKAGES_KEY }}
      - name: Assembly
        run: mvn --settings .github/workflows/settings.xml -B -P github --projects nightglow-cli assembly:single
        env:
          MAVEN_USER: ${{ secrets.READ_PACKAGES_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.READ_PACKAGES_KEY }}
      - name: Upload zip
        uses:  actions/upload-artifact@v2
        with:
          name: nightglow-cli
          path: nightglow-cli/target/*.zip
      - name: Upload tar.gz
        uses:  actions/upload-artifact@v2
        with:
          name: nightglow-cli
          path: nightglow-cli/target/*.tar.gz
